name: Purge Camo Cache
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 * * * *' # Every hour
  workflow_dispatch:

jobs:
  purge-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Purge Camo Cache
        uses: kevincobain2000/action-camo-purge@v1
